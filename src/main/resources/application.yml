logging:
  file:
    path: /file/log/
  register-shutdown-hook: true

spring:
  datasource: 
    url: jdbc:mysql://34.151.202.5/comunidad?allowPublicKeyRetrieval=true&useSSL=false&useTimezone=true&serverTimezone=GMT&characterEncoding=UTF-8
    username: root
    password: 2P0r0e1n
    driver-class-name: com.mysql.cj.jdbc.Driver 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database: mysql 
  thymeleaf:
   cache: false 



   

